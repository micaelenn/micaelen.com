import { client } from '@/utils/config/client'
import { schemas } from '@/utils/config/schemas'

// {{upperCase elementName}} CONTENT
export const get{{ pascalCase elementName }}Data = async () => {
    return await client.fetch(
       `*[_type == "${schemas.}"][0] {

       }`
    )
}