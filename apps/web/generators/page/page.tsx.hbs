export const dynamic = 'force-dynamic'

// internal dependencies
import {{ pascalCase elementName }}Layout from "@/layouts/{{ pascalCase elementName }}/{{ pascalCase elementName }}";
import { get{{ pascalCase elementName }}Data } from "@/lib/sanity/{{ kebabCase elementName }}";
import { getSeo } from "@/lib/sanity/seo"

export async function generateMetadata() {
  const seo = await getSeo('{{ lowerCase elementName }}')

  return {
    title: seo.title,
    description: seo.description
  }
}

export default async function {{ pascalCase elementName }}() {
  const {{ camelCase elementName }}Data = await get{{ pascalCase elementName }}Data()

  return (
    <{{pascalCase elementName}}Layout {...{{ camelCase elementName }}Data} />
  )
}