"use client"

// external dependencies
import React, { FC } from 'react'

// internal dependencies
import { Container } from './{{pascalCase elementName}}.styles'

// types
interface {{pascalCase elementName}}LayoutProps {
 
}

const {{pascalCase elementName}}Layout: FC<{{pascalCase elementName}}LayoutProps> = (data: any) => {
  return (
    <Container className="container">
        
    </Container>
	);
};

export default {{pascalCase elementName}}Layout;
