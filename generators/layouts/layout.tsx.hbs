// external dependencies
import React, { FC } from 'react';

// internal dependencies
import { Container } from './{{elementName}}.styles'

// types
interface {{elementName}}LayoutProps {
 
}

const {{elementName}}Layout: FC<{{elementName}}LayoutProps> = ({}) => {
  return (
    <Container>
        
    </Container>
	);
};

export default {{elementName}}Layout;
