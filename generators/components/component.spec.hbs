import { render, screen } from '@testing-library/react';
import {{pascalCase elementName}} from './{{pascalCase elementName}}';

function renderComponent() {
return render(
<{{pascalCase elementName}} />
)
}

describe('{{pascalCase elementName}}', () => {
it("should render the '{{pascalCase elementName}}' component", () => {
renderComponent()
})
})