"use client"

// external dependencies
import React, { FC } from 'react'

// internal dependencies
import { Container } from './Note.styles'

// types
interface NoteLayoutProps {
 
}

const NoteLayout: FC<NoteLayoutProps> = (data: any) => {
  return (
    <Container>
        
    </Container>
	);
};

export default NoteLayout;
